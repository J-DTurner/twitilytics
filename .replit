modules = ["nodejs-20", "web", "bash"]
run = "cd backend && npm run dev & cd .. & cd frontend && npm start"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd backend && npm run dev & cd .. & cd frontend && npm start"]

[[ports]]
localPort = 8001
externalPort = 80
